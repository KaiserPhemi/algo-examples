/**
 * @author Oluwafemi Akinwa
 * @desc sets a mid point 
 * @param {int} start 
 * @param {int} end 
*/
const setMiddle = (start, end) => {
  return Math.floor((start + end) / 2);
};

module.exports = setMiddle;
