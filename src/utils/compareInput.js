/**
 * @author Oluwafemi Akinwa
 * @desc compares two int inputs to use in sorting
 * @param {int} firstInput 
 * @param {int} secondInput 
 */
const compareInput = (firstInput, secondInput) => {
  return firstInput - secondInput;
};

module.exports = compareInput;
